{
  "name": "photato-website",
  "version": "1.0.0",
  "scripts": {
    "start": "cd client && npm start",
    "client": "cd client && npm start",
    "server": "cd server && nodemon local-server.js",
    "start-server-and-client": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\""
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
